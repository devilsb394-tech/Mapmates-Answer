import { Express } from "express";
import * as cheerio from 'cheerio';
import { callAIWithFallback } from "../lib/ai.js";

export function setupApiRoutes(app: Express) {
  // We will move routes here.
}
